# WooCommerce-SKU-Image-Updater
Upload one or more images, enter the SKU that matches each one, and the plugin will find the product (simple, variable, or a specific variation), replace its featured image, and regenerate WordPress image sizes automatically.
